name: One-shot Setup Center button UI

on:
  push:
    branches:
      - automation/setup-center-button-ui

permissions:
  contents: write

concurrency:
  group: one-shot-setup-center-button-ui
  cancel-in-progress: false

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Fix generated Setup Center UI and remove transport automation
        shell: bash
        run: |
          set -euo pipefail
          git pull --ff-only origin main
          git config user.name Eabusham2
          git config user.email eabusham2@gmail.com
          python3 .github/scripts/apply-setup-center-button-ui.py
          git push origin HEAD:main
          git push origin --delete automation/setup-center-button-ui || true
