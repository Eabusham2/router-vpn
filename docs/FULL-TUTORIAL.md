# Router VPN — full tutorial

The maintained full tutorial is:

```text
docs/CURRENT-GUIDE.md
```

This file is intentionally a stable compatibility pointer so two large tutorials cannot drift apart.

Use `docs/CURRENT-GUIDE.md` for the current image-only Portainer server deployment, GitHub-first/router-local **client-package** fallback, Setup Center, ASUS forwarding, client downloads, logical modes, DNS, platform boundaries, diagnostics and off-LAN testing.
